<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jlumart.mapper.CategoryMapper">
    <update id="update">
        update categories
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId},
            </if>
            <if test="sortWeight != null">
                sort_weight = #{sortWeight},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>
    <select id="pageQuery" resultType="com.jlumart.vo.CategoryPageVO">
        select
        c.id id,
        c.name name,
        c.parent_id parent_id,
        p.name parent_category,
        c.sort_weight sort_weight,
        c.status status,
        c.update_time update_time
        from categories c
        left join categories p on c.parent_id = p.id
        <where>
            <if test="name != null and name != ''">
                and c.name like '%' || #{name} || '%'
            </if>
            <if test="parentId != null">
                and c.parent_id = #{parentId}
            </if>
            <if test="status != null">
                and c.status = #{status}
            </if>
        </where>
        order by c.sort_weight, c.create_time desc
    </select>
</mapper>